let reqlib = require("app-root-path").require;
let { expect } = require("chai");
let { ImageRenderer } = reqlib("/src/animation/renderers/image/ImageRenderer");

describe("ImageRenderer", () => {
    it("should import correctly", () => {});
});
